enum Role {
  USER
  ADMIN
}

enum ParticipationStatus {
  PENDING
  APPROVED
  REJECTED
  BANNED
}

enum PaymentStatus {
  PAID
  UNPAID
}

enum InvitationStatus {
  PENDING
  ACCEPTED
  DECLINED
}

enum EventType {
  ONLINE
  OFFLINE
}
